Adjusted B3 prices are transformed into a synchronized return panel. Random Matrix Theory separates collective market, group-level and noise-compatible components. Matched MST and PMFG networks expose the corresponding topology, which is then compared with retrospective volatility-model performance because the structural descriptors use the complete synchronized sample.
