# Copy this file to config/clickhouse.toml and set your local credentials.
# Alternatively, set B3_ECONOPHYSICS_CONFIG to another TOML file and/or use
# B3_CH_HOST, B3_CH_PORT, B3_CH_USERNAME, B3_CH_PASSWORD and B3_CH_DATABASE.

[clickhouse]
host = "localhost"
port = 8123
username = "default"
password = "replace-with-a-local-password"
database = "quantbase"

[tables]
daily = "quantbase.candles_1d"
